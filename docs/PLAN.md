# Keel — status (v0.4.1)

v0.4 features plus **token slim**: SKILL laws + Does/Does not table, single Close (next-commands), critique lazy-load (`critique-scoring.md`, modes on demand), Close footers removed from playbooks, `new-work` demoted to routing-only.

## Verify

```bash
npm test && npm run sync
wc -c skill/SKILL.md skill/reference/critique.md
```
